{
	"private": true,
	"name": "journal",
	"description": "homepage of rayraegah",
	"scripts": {
		"serve": "docker-compose up",
		"build": "npm run clean && npm run build:js && npm run build:css",
		"watch:js": "npx parcel watch ./src/js/main.js --public-url /dist/ --out-dir www/dist --cache-dir vendor/parcel --no-content-hash",
		"watch:css": "npx parcel watch ./src/scss/style.scss --public-url /dist/ --out-dir www/dist --cache-dir vendor/parcel --no-content-hash",
		"build:js": "npx parcel build ./src/js/main.js --public-url /dist/ --out-dir www/dist --cache-dir vendor/parcel --no-content-hash",
		"build:css": "npx parcel build ./src/scss/style.scss --public-url /dist/ --out-dir www/dist --cache-dir vendor/parcel --no-content-hash",
		"clean": "rm -rf www/dist"
	},
	"devDependencies": {
		"cssnano": "^4.1.10",
		"sass": "^1.20.1"
	}
}
